// Hardcoded dev default so the directory/project engines have something real
// to open immediately — a proper "Open Folder"/"Open Solution" flow is a
// natural follow-up, not required to prove the pipeline end-to-end.
export const DEV_SOLUTION_PATH = "/home/f000nk/Projects/Yog-Mod-Loader";
