// Hardcoded dev default so the directory/project engines have something real
// to open immediately — a proper "Open Folder"/"Open Project" flow is a
// natural follow-up, not required to prove the pipeline end-to-end. Points
// at a real `yog.toml` mod project (not Yog-Mod-Loader — that's a build
// dependency, not something opened as its own project here).
export const DEV_PROJECT_PATH = "/home/f000nk/Projects/Yog-Modlist";
